class Solution {
public:
    double myPow(double x, int n) {
        if(n==0) return 1.0;
        if(n==1) return x;
        if(n<0)  return 1/myPow(x,-n);
        if(n%2 == 0) return pow(x,n>>1)*pow(x,n>>1);
        else return pow(x,n>>1)*pow(x,n>>1)*x;
    }
};
